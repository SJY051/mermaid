---
title: mermAid 테이블 명세서
status: generated
created: 2026-07-10
source: MariaDB 11.4 INFORMATION_SCHEMA, after Flyway V1–V3
---

# 테이블 명세서

> **실제 데이터베이스에서 생성했습니다.** `./bin/gen-erd.py`로 재생성하세요.
> `docs/deliverables/ERD.md`와 같은 스키마이며, JPA `ddl-auto: validate`가 통과합니다.

- DBMS: **MariaDB 11.4**, 엔진 `InnoDB`, 문자셋 `utf8mb4`
- 마이그레이션: `backend/src/main/resources/db/migration/`
- 모든 시각 컬럼은 `DATETIME(6)` UTC이며 JPA Auditing이 채웁니다

**의료 상담 대화는 이 스키마에 없습니다.** 브라우저 `sessionStorage`에만 존재합니다 (spec §2-4, §2-16).

## `allergy_ingredient`

> 기피 성분. 동의(opt-in)한 경우에만 저장됨 (FR-04)

| # | 컬럼 | 타입 | NULL | 기본값 | 키 | 기타 | 설명 |
|---:|---|---|:---:|---|:---:|---|---|
| 1 | `id` | `bigint(20)` | N | - | PK | auto_increment | - |
| 2 | `profile_id` | `bigint(20)` | N | - | FK | - | - |
| 3 | `ingredient_name_en` | `varchar(255)` | N | - | - | - | - |
| 4 | `ingredient_name_ko` | `varchar(255)` | Y | - | - | - | - |
| 5 | `normalized_key` | `varchar(255)` | Y | - | - | - | Lower-cased, dose-stripped form used for matching against MAIN_INGR_ENG. |
| 6 | `confidence` | `varchar(20)` | N | `'UNKNOWN'` | - | - | EXACT | SYNONYM | PARTIAL | UNKNOWN. Only EXACT and SYNONYM may block a drug. |
| 7 | `created_at` | `datetime(6)` | N | - | - | - | - |

**외래키**

| 제약조건 | 컬럼 | 참조 |
|---|---|---|
| `fk_allergy_profile` | `profile_id` | `user_profile.id` ON DELETE CASCADE |

**인덱스**

| 이름 | 컬럼 | 유일 |
|---|---|---|
| `ix_allergy_normalized_key` | `normalized_key` | N |
| `PRIMARY` | `id` | Y |
| `uk_allergy_profile_ingredient` | `profile_id,ingredient_name_en` | Y |

## `favorite_facility`

> 즐겨찾기한 약국·병원. CRUD의 C/R/U/D 시연 대상

| # | 컬럼 | 타입 | NULL | 기본값 | 키 | 기타 | 설명 |
|---:|---|---|:---:|---|:---:|---|---|
| 1 | `id` | `bigint(20)` | N | - | PK | auto_increment | - |
| 2 | `profile_id` | `bigint(20)` | N | - | FK | - | - |
| 3 | `facility_id` | `varchar(255)` | N | - | - | - | Provider-namespaced id, e.g. facility:nmc:C1110693. Never a name. |
| 4 | `facility_type` | `varchar(20)` | N | - | - | - | PHARMACY | HOSPITAL |
| 5 | `alias` | `varchar(100)` | Y | - | - | - | User-chosen label, e.g. "Near my hotel". |
| 6 | `memo` | `varchar(500)` | Y | - | - | - | - |
| 7 | `created_at` | `datetime(6)` | N | - | - | - | - |
| 8 | `updated_at` | `datetime(6)` | N | - | - | - | - |

**외래키**

| 제약조건 | 컬럼 | 참조 |
|---|---|---|
| `fk_favorite_profile` | `profile_id` | `user_profile.id` ON DELETE CASCADE |

**인덱스**

| 이름 | 컬럼 | 유일 |
|---|---|---|
| `PRIMARY` | `id` | Y |
| `uk_favorite_profile_facility` | `profile_id,facility_id` | Y |

## `user_profile`

> 익명 프로필. 계정이 아니라 브라우저가 만든 UUID (spec §2-5)

| # | 컬럼 | 타입 | NULL | 기본값 | 키 | 기타 | 설명 |
|---:|---|---|:---:|---|:---:|---|---|
| 1 | `id` | `bigint(20)` | N | - | PK | auto_increment | - |
| 2 | `device_id` | `varchar(36)` | N | - | UK | - | Client-generated UUID. Not a user account. |
| 3 | `country_code` | `varchar(2)` | Y | - | - | - | ISO 3166-1 alpha-2, drives FR-05 guidance. |
| 4 | `remember_allergies` | `tinyint(1)` | N | `0` | - | - | Opt-in. FALSE means allergies live only in the browser session. |
| 5 | `created_at` | `datetime(6)` | N | - | - | - | - |
| 6 | `updated_at` | `datetime(6)` | N | - | - | - | - |

**인덱스**

| 이름 | 컬럼 | 유일 |
|---|---|---|
| `PRIMARY` | `id` | Y |
| `uk_user_profile_device_id` | `device_id` | Y |

---

## 설계 근거

**`user_profile.remember_allergies`**
알레르기는 기본적으로 세션 입력입니다. 사용자가 명시적으로 켰을 때만 이 DB에 저장되고,
끄면 저장된 행이 즉시 삭제됩니다 (spec §2-5). 이 컬럼이 그 약속을 검증 가능하게 만듭니다.

**`allergy_ingredient.normalized_key` / `confidence`**
"Ibuprofen", "ibuprofen 200mg", "Ibuprofen (Advil)"은 같은 성분입니다. 정규화된 키로 비교하고,
그 정규화가 정확 일치인지 검토된 동의어인지 추측인지를 `confidence`에 남깁니다.
**추측(`PARTIAL`/`UNKNOWN`)은 약을 차단하지 못합니다** — 경고만 냅니다 (spec §2-12).

**`favorite_facility.facility_id`**
`facility:nmc:C1110693` 처럼 공급자 네임스페이스를 포함합니다 (spec §4-3).
약국은 `hpid`, 병원은 `ykiho`이며 서로 다른 기관이 발급하므로 접두사가 필요합니다.
**이름이나 주소를 ID로 쓰지 않습니다.**
