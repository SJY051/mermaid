# R05-CAN-005 — Extraction omits the provider no-storage flag

- Candidate ID: R05-CAN-005
- Title: Extraction omits the provider no-storage flag
- Instance key: extraction-provider-retention:backend/src/main/java/com/mermaid/chat/ChatProxyService.java:115
- Ledger row ID: central-validation-round-06:R05-CAN-005

## Attack Path Steps

1. Source: Anonymous user's newest consultation message sent verbatim to the configured provider for term extraction.
2. Control gap: The application avoids logging the text and sets store=false on pass 2, but pass 1 relies on the provider's unspecified default.
3. Sink: Sensitive symptom text may be eligible for provider-side storage even though the sibling completion path explicitly opts out.
4. Impact: Consultation content can persist at the third-party LLM provider contrary to the intended ephemeral handling, depending on provider semantics.

## Attack Path Facts

- Context: Extraction omits the provider no-storage flag. Repository evidence verifies the stated source-to-sink trust-boundary crossing; any remaining proof gap concerns consequence or policy, not the static data flow. Source: Anonymous user's newest consultation message sent verbatim to the configured provider for term extraction. Sink: Sensitive symptom text may be eligible for provider-side storage even though the sibling completion path explicitly opts out.
- In-scope status: in_scope
- In-scope reasoning: The immutable target contains the cited privacy / provider retention source, control, and sink. The structured facts separately record boundary behavior, actor authority, and unresolved consequence evidence.
- Exposure: public
- Identity: The user is the consultation data subject and source; the configured LLM provider is the recipient with provider-side retention authority.
- Cross-boundary behavior: verified
- Vector: remote
- Precondition feasibility: plausible
- Attacker input control: no
- Category: Privacy / provider retention
- Auth scope: public
- Impact surface: data
- Target reach: single_service
- Confidence: 0.55

### Assumptions
- The immutable target commit is the analyzed product and repository surface for R05-CAN-005.
- The configured compatible provider interprets an omitted store field as permitting persistence and extraction is invoked with consultation text.

### Exposure Evidence
- artifacts/01_context/threat_model.md | 101-110 | Anonymous consultation users can submit remote text without authentication.
- backend/src/main/java/com/mermaid/chat/ChatProxyService.java | 115-133 | The extraction request sends raw newest-user text but does not set store=false.

### Identity Evidence
- artifacts/01_context/threat_model.md | 113-121 | The browser user is the data subject, and same-origin browser code can access browser-local state.
- artifacts/01_context/threat_model.md | 123-130 | The external LLM/provider controls model output while anonymous user text can influence that output.
- backend/src/main/java/com/mermaid/chat/SearchTermExtractor.java | 112-120 | Victim symptom and medicine text is sent for extraction.

### Cross-boundary Evidence
- artifacts/01_context/threat_model.md | 172-180 | LLM output crosses an external trust boundary into first-party medical and safety rendering.
- backend/src/main/java/com/mermaid/chat/ChatProxyService.java | 115-133 | The victim's newest message leaves the service in a provider request without store=false.
- backend/src/main/java/com/mermaid/chat/ChatProxyService.java | 195-202 | The sibling pass-2 request explicitly opts out of storage.

### Vector Evidence
- artifacts/01_context/threat_model.md | 101-110 | Anonymous consultation users can submit remote text without authentication.
- backend/src/main/java/com/mermaid/chat/ChatProxyService.java | 115-133 | The extraction request sends raw newest-user text but does not set store=false.

### Preconditions
- The configured compatible provider interprets an omitted store field as permitting persistence and extraction is invoked with consultation text.

### Attacker Input Evidence
- backend/src/main/java/com/mermaid/chat/SearchTermExtractor.java | 112-120 | The transmitted text belongs to the victim; provider retention is the unresolved risk.

### Mitigations Already Present
- The application does not log the extraction text, pass 2 explicitly sends store=false, and a provider may default to no storage or retain abuse logs regardless of this field.

### Auth Scope Evidence
- artifacts/01_context/threat_model.md | 123-130 | The LLM/provider is an external actor influenced by attacker-controlled consultation text.

### Impact Surface Evidence
- backend/src/main/java/com/mermaid/chat/ChatProxyService.java | 115-133 | The verified data transfer reaches the provider; actual persistence depends on provider semantics.

### Target Reach Evidence
- backend/src/main/java/com/mermaid/chat/SearchTermExtractor.java | 112-120 | The path applies to anonymous consultations invoking extraction.

### Secrets References
- None.

### Blindspots
- The configured provider's default storage and retention contract is not present in target objects.

### Controls
- The application avoids logging the text and sets store=false on pass 2, but pass 1 relies on the provider's unspecified default.

## Affected Locations

1. backend/src/main/java/com/mermaid/chat/ChatProxyService.java
   - lines: 115-133
   - label: root_control
   - detail: The extraction request sends raw newest-user text but does not set store=false.
2. backend/src/main/java/com/mermaid/chat/ChatProxyService.java
   - lines: 195-202
   - label: contrasting_privacy_control
   - detail: The full-answer request explicitly sets store=false.
3. backend/src/main/java/com/mermaid/chat/SearchTermExtractor.java
   - lines: 112-120
   - label: sensitive_source
   - detail: The text sent can contain symptoms and medicine questions.

## Counterevidence

- The application does not log the extraction text, pass 2 explicitly sends store=false, and a provider may default to no storage or retain abuse logs regardless of this field.

## Severity Calibration

- Impact level: medium
- Likelihood level: unknown
- Critical criteria satisfied: false
- Hard suppression: none
- Hard suppression rationale: No hard suppression applies: the structured facts are in scope and their preconditions are not unachievable; likelihood and impact are handled only by the mechanical matrix.
- Calibrated severity: low

## AGENTS Repository Priority Axis

- Repository validation disposition: needs_review
- Repository priority: P0
- Repository review tier: conditional P0: required clinical, provider, deployment, or runtime proof is unresolved
- Conditional: yes
- Repository-axis rationale: Pass 1 sends symptom text without the sibling no-storage flag, but omission does not itself prove third-party persistence. If provider semantics permit storage this bypasses §2-5 and is P0; provider verification is required.
- Axis basis: artifacts/01_context/threat_model.md:319-322 requires generic security severity and AGENTS repository priority to be reported separately.
- Axis separation: repository priority P0 is not mechanically translated into generic attack-path final priority P3.

## Final Policy Decision

- Final policy decision: deferred
- Final priority: P3
- Rationale: Pass 1 sends symptom text without the sibling no-storage flag, but omission does not itself prove third-party persistence. If provider semantics permit storage this bypasses §2-5 and is P0; provider verification is required. The mechanical medium/unknown matrix result is low, but final policy remains deferred until the recorded proof gap is resolved.

### Proof Gaps
- The configured provider's default storage and retention contract is not present in target objects.
