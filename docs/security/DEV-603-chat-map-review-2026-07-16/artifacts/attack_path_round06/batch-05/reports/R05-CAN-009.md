# R05-CAN-009 — Global gitleaks value allowlist accepts credential-shaped storage keys

- Candidate ID: R05-CAN-009
- Title: Global gitleaks value allowlist accepts credential-shaped storage keys
- Instance key: gitleaks-secret-shape-allowlist:.gitleaks.toml:27
- Ledger row ID: central-validation-round-06:R05-CAN-009

## Attack Path Steps

1. Source: A contributor-controlled or operator-chosen secret value that matches mermaid.<letters>.v<digits>, placed in a credential-like assignment in any staged file.
2. Control gap: The allowlist is value-scoped rather than path-scoped, but the value language is broader than the finite browser-storage constants it intends to exempt.
3. Sink: A default gitleaks finding whose extracted secret matches the regex can be globally suppressed before a public commit.
4. Impact: A matching real credential could enter public Git history without the repository's secret scan stopping it.

## Attack Path Facts

- Context: Global gitleaks value allowlist accepts credential-shaped storage keys. The target supports a plausible source-to-sink trust-boundary crossing, while the recorded proof gap prevents treating its consequence as verified. Source: A contributor-controlled or operator-chosen secret value that matches mermaid.<letters>.v<digits>, placed in a credential-like assignment in any staged file. Sink: A default gitleaks finding whose extracted secret matches the regex can be globally suppressed before a public commit.
- In-scope status: in_scope
- In-scope reasoning: The immutable target contains the cited secret-scanning bypass source, control, and sink. The structured facts separately record boundary behavior, actor authority, and unresolved consequence evidence.
- Exposure: internal
- Identity: A contributor or operator supplies a credential-shaped repository value; gitleaks executes with CI and public-history gate authority.
- Cross-boundary behavior: plausible
- Vector: unknown
- Precondition feasibility: unknown
- Attacker input control: plausible
- Category: Secret-scanning bypass
- Auth scope: internal_only
- Impact surface: build
- Target reach: single_service
- Confidence: 0.52

### Assumptions
- The immutable target commit is the analyzed product and repository surface for R05-CAN-009.
- A real secret value matches mermaid.<letters>.v<digits>, a gitleaks detector would otherwise flag it, and a staged file is scanned with the target allowlist.

### Exposure Evidence
- artifacts/01_context/threat_model.md | 149-157 | The affected scanner policy is an internal contribution and CI surface.
- .gitleaks.toml | 19-27 | A repository-wide secret-value allowlist accepts mermaid.<letters>.v<digits> with unbounded letter and digit lengths and no finite storage-key name set.

### Identity Evidence
- artifacts/01_context/threat_model.md | 149-157 | Developers, delegated agents, and CI execute repository tooling with local source-control authority.
- .gitleaks.toml | 19-27 | The repository scanner configuration globally exempts the matching value grammar.

### Cross-boundary Evidence
- artifacts/01_context/threat_model.md | 202-208 | Repository configuration and policy mediate content or commands entering developer and CI execution state.
- .gitleaks.toml | 19-27 | A matching value can plausibly cross the secret-scanning gate into commit history.

### Vector Evidence
- .gitleaks.toml | 19-27 | The target proves an internal scanner rule but no public PR or network ingress vector.

### Preconditions
- A real secret value matches mermaid.<letters>.v<digits>, a gitleaks detector would otherwise flag it, and a staged file is scanned with the target allowlist.

### Attacker Input Evidence
- .gitleaks.toml | 19-27 | A contributor or operator can plausibly choose a value matching the exempt grammar.

### Mitigations Already Present
- Common provider-issued keys do not use this format, the regex is intended for browser-storage constants, and no real secret suppression was demonstrated.

### Auth Scope Evidence
- artifacts/01_context/threat_model.md | 149-157 | Developers, delegated agents, and CI execute repository tooling with local source-control authority.

### Impact Surface Evidence
- .gitleaks.toml | 19-27 | The allowlist acts at the repository secret-detection gate.

### Target Reach Evidence
- .gitleaks.toml | 19-27 | The rule is repository-wide rather than bound to finite storage-key paths.

### Secrets References
- Credential-shaped values matching mermaid.<letters>.v<digits>; no real credential value is reproduced.

### Blindspots
- A target-version gitleaks reproduction must show an otherwise-detected credential being suppressed by this value allowlist.

### Controls
- The allowlist is value-scoped rather than path-scoped, but the value language is broader than the finite browser-storage constants it intends to exempt.

## Affected Locations

1. .gitleaks.toml
   - lines: 19-27
   - label: root_control
   - detail: A repository-wide secret-value allowlist accepts mermaid.<letters>.v<digits> with unbounded letter and digit lengths and no finite storage-key name set.

## Counterevidence

- Common provider-issued keys do not use this format, the regex is intended for browser-storage constants, and no real secret suppression was demonstrated.

## Severity Calibration

- Impact level: high
- Likelihood level: unknown
- Critical criteria satisfied: false
- Hard suppression: none
- Hard suppression rationale: No hard suppression applies: the structured facts are in scope and their preconditions are not unachievable; likelihood and impact are handled only by the mechanical matrix.
- Calibrated severity: medium

## AGENTS Repository Priority Axis

- Repository validation disposition: needs_review
- Repository priority: P0
- Repository review tier: conditional P0: required clinical, provider, deployment, or runtime proof is unresolved
- Conditional: yes
- Repository-axis rationale: The regex is globally value-scoped and broader than its stated constants, but regex overlap alone does not prove a real detector bypass. A demonstrated secret bypass would violate §2-8 and be P0, so the candidate needs review.
- Axis basis: artifacts/01_context/threat_model.md:319-322 requires generic security severity and AGENTS repository priority to be reported separately.
- Axis separation: repository priority P0 is not mechanically translated into generic attack-path final priority P2.

## Final Policy Decision

- Final policy decision: deferred
- Final priority: P2
- Rationale: The regex is globally value-scoped and broader than its stated constants, but regex overlap alone does not prove a real detector bypass. A demonstrated secret bypass would violate §2-8 and be P0, so the candidate needs review. The mechanical high/unknown matrix result is medium, but final policy remains deferred until the recorded proof gap is resolved.

### Proof Gaps
- A target-version gitleaks reproduction must show an otherwise-detected credential being suppressed by this value allowlist.
