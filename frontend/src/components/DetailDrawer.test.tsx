import { describe, it } from 'vitest'

describe('DetailDrawer', () => {
  it.todo('shows the selected facility name, type, address, distance, and source')
  it.todo('offers a telephone link when the facility has a phone number')
  it.todo('renders null opening hours as "Hours unknown", never "Closed"')
  it.todo('closes through an accessible close button')
  it.todo('exposes the bottom sheet as an accessible dialog')
})
