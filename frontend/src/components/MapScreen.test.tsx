import { describe, it } from 'vitest'

describe('MapScreen', () => {
  it.todo('renders null opening hours as "Hours unknown", never "Closed"')
  it.todo('splits open-now results client-side so unknown hours remain available to call')
})
